% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{ht_set_log_level}
\alias{ht_set_log_level}
\alias{ht_enable_log_capture}
\alias{ht_log_history}
\alias{ht_clear_log_history}
\title{Control logging for heteroTests diagnostics}
\usage{
ht_set_log_level(level = c("INFO", "WARN", "ERROR", "SILENT"))

ht_enable_log_capture(enabled = TRUE, max_entries = 1000L, sink = NULL)

ht_log_history()

ht_clear_log_history()
}
\arguments{
\item{level}{Character string specifying the minimum level to emit. Accepted values are
"INFO", "WARN", "ERROR" and "SILENT".}

\item{enabled}{Logical flag, \code{TRUE} to enable capture and \code{FALSE} to disable.}

\item{max_entries}{Maximum number of entries to retain in memory. Older entries are discarded first.
Use \code{Inf} to keep all entries.}

\item{sink}{Optional file path or connection to mirror log output.}
}
\value{
\code{ht_set_log_level()} returns the previous log level invisibly.
\code{ht_enable_log_capture()} and \code{ht_clear_log_history()} return \code{NULL}.
\code{ht_log_history()} returns a data frame with columns \code{timestamp}, \code{level} and \code{message}.
}
\description{
These helpers manage the package-level logging subsystem used to debug complex
diagnostic workflows. Users can adjust verbosity, enable log capture, inspect
recent log entries and clear the stored history when finished.
}
