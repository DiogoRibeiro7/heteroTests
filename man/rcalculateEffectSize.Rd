% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect_sizes.R
\name{rcalculateEffectSize}
\alias{rcalculateEffectSize}
\title{Effect size calculations for heteroscedasticity diagnostics}
\usage{
rcalculateEffectSize(
  test_result,
  model,
  data,
  type = c("cramers_v", "phi", "eta_squared")
)
}
\arguments{
\item{test_result}{Result object from a heteroscedasticity test (typically
of class \code{htest}).}

\item{model}{Fitted model supplied to the diagnostic.}

\item{data}{Data frame containing the variables referenced in \code{model}.}

\item{type}{Effect size metric to compute. Supported options are
\code{"cramers_v"}, \code{"phi"}, and \code{"eta_squared"}.}
}
\value{
A named list with elements \code{effect_size}, \code{magnitude},
\code{practical_significance}, \code{interpretation}, and \code{type}.
}
\description{
Converts chi-squared statistics produced by heteroscedasticity tests into
interpretable effect sizes such as Cramer's V, the phi coefficient, or an
eta-squared analogue. The helper also provides qualitative magnitude
descriptors and a brief interpretation string that can be surfaced to users.
}
\examples{
data(mtcars)
model <- lm(mpg ~ wt + cyl, data = mtcars)
result <- performWhiteTest(model, mtcars)
rcalculateEffectSize(result, model, mtcars)

}
