% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modern_diagnostics.R
\name{performHCCovarianceTest}
\alias{performHCCovarianceTest}
\title{HC-type covariance heteroscedasticity test}
\usage{
performHCCovarianceTest(
  model,
  data,
  type = c("HC0", "HC1", "HC2", "HC3", "HC4")
)
}
\arguments{
\item{model}{A fitted \link[stats:lm]{stats::lm} object describing the mean structure whose
residual variance is to be assessed.}

\item{data}{A \link[base:data.frame]{base::data.frame} (or compatible object) containing the
variables referenced in \code{model}. The data must include all observations used
to fit \code{model} and should not contain unresolved missing values.}

\item{type}{Character string selecting the covariance adjustment. Supported
values are "HC0", "HC1", "HC2", "HC3", and "HC4".}
}
\value{
An \link[stats:htest]{htest} object with the adjusted Lagrange multiplier statistic.
}
\description{
Uses heteroscedasticity-consistent covariance matrix adjustments (HC0–HC4) to
stabilise the auxiliary regression employed by the Breusch-Pagan statistic.
Large departures between robust and homoskedastic variance estimates manifest
as a significant chi-squared statistic signalling heteroscedastic errors.
}
\examples{
data(mtcars)
model <- lm(mpg ~ wt + hp, data = mtcars)
performHCCovarianceTest(model, mtcars, type = "HC3")

}
\references{
MacKinnon, J. G., & White, H. (1985). Some heteroskedasticity-consistent
covariance matrix estimators with improved finite sample properties. \emph{Journal
of Econometrics, 29}(3), 305–325.

Long, J. S., & Ervin, L. H. (2000). Using heteroscedasticity consistent
standard errors in the linear regression model. \emph{The American Statistician,
54}(3), 217–224.
}
