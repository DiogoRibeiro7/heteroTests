% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{rhandleMissingValues}
\alias{rhandleMissingValues}
\title{Handle missing values according to a specified strategy}
\usage{
rhandleMissingValues(data, variables, strategy = "complete_cases")
}
\arguments{
\item{data}{A data.frame containing the data to be processed.}

\item{variables}{Character vector of variable names to inspect for missing
values.}

\item{strategy}{Strategy describing how missing values should be handled.
The options are:
\itemize{
\item \code{"complete_cases"} – remove incomplete rows and emit a warning that
summarizes the data loss.
\item \code{"warn"} – behaves identically to \code{"complete_cases"} but is provided as a
semantic alias when a calling test wants to emphasize the warning
behaviour explicitly.
\item \code{"fail"} – abort when any missing values are detected.
}}
}
\value{
A list with components \code{data} (the processed data frame),
\code{removed_cases} (row indices removed), \code{removed_count} (number of removed
observations), \code{removed_fraction} (proportion removed relative to the
original data), \code{removed_variables} (variables with observed missingness),
and \code{loss_message} (the formatted warning text).
}
\description{
Provides a centralized mechanism for dealing with missing values in
heteroscedasticity tests. The function can either drop incomplete cases,
emit warnings, or fail fast when missingness is not permitted.
}
\examples{
cleaned <- rhandleMissingValues(mtcars, c("mpg", "wt"))
cleaned$removed_count
}
\keyword{internal}
