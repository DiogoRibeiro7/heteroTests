% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modern_diagnostics.R
\name{performHighDimensionalTest}
\alias{performHighDimensionalTest}
\title{High-dimensional projection heteroscedasticity test}
\usage{
performHighDimensionalTest(
  model,
  data,
  variance_threshold = 0.9,
  max_components = NULL
)
}
\arguments{
\item{model}{A fitted \link[stats:lm]{stats::lm} object describing the mean structure whose
residual variance is to be assessed.}

\item{data}{A \link[base:data.frame]{base::data.frame} (or compatible object) containing the
variables referenced in \code{model}. The data must include all observations used
to fit \code{model} and should not contain unresolved missing values.}

\item{variance_threshold}{Proportion of predictor variance that the selected
principal components should explain. Defaults to 0.9.}

\item{max_components}{Optional upper bound on the number of principal
components to retain. Defaults to \code{min(10, n - 5)}.}
}
\value{
An \link[stats:htest]{htest} object summarising the chi-squared statistic
of the auxiliary regression on principal component scores.
}
\description{
Applies principal component projections to the design matrix and evaluates a
Breusch-Pagan style statistic on the reduced representation. The procedure is
designed for settings where the number of predictors rivals or exceeds the
sample size.
}
\examples{
set.seed(123)
X <- matrix(rnorm(80 * 20), nrow = 80)
beta <- c(rep(1, 5), rep(0, 15))
y <- X \%*\% beta + rnorm(80, sd = 0.5 + 0.2 * scale(X[, 1]))
df <- as.data.frame(cbind(y = as.numeric(y), X))
model <- lm(y ~ ., data = df)
performHighDimensionalTest(model, df)

}
\references{
Fan, J., & Lv, J. (2008). Sure independence screening for ultrahigh dimensional
feature space. \emph{Journal of the Royal Statistical Society: Series B, 70}(5),
849–911.
}
