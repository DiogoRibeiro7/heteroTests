% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{rvalidateDistributionalAssumptions}
\alias{rvalidateDistributionalAssumptions}
\title{Validate distributional assumptions}
\usage{
rvalidateDistributionalAssumptions(data, assumptions = list())
}
\arguments{
\item{data}{A data.frame containing the variables required for assessment.}

\item{assumptions}{A named list describing the desired checks. Recognised
entries are:
\itemize{
\item \code{normality} – character vector or list with element \code{variables}
specifying which columns should satisfy approximate normality. Optional
list elements \code{alpha} (significance level) and \code{sample_limit} (maximum
sample size for the Shapiro–Wilk test) can be supplied.
\item \code{positive} – character vector or list identifying variables that
must contain positive values. When provided as a list, \code{test_name}
overrides the label used in error messages.
\item \code{variation} – character vector or list (with \code{variables} and
optional \code{tolerance}) describing variables that must exhibit
non-negligible variance.
\item \code{outliers} – character vector or list of variables to screen for
extreme observations. Lists may include a numeric \code{threshold}
specifying the acceptable number of robust standard deviations.
}}
}
\value{
A list containing \code{passed} (logical flag), \code{messages} (character
vector of violations), \code{warnings} (character vector of recoverable issues),
and \code{details} (named list with diagnostic information for each assumption).
}
\description{
Assesses frequently used distributional assumptions before running
heteroscedasticity diagnostics. The checks cover approximate normality,
positivity for log-transformed quantities, sufficient variation, and the
presence of extreme outliers.
}
\examples{
res <- heteroTests:::rvalidateDistributionalAssumptions(
  mtcars,
  assumptions = list(
    normality = list(variables = "mpg"),
    positive = list(variables = "disp", test_name = "Demo Test")
  )
)
res$passed
}
\keyword{internal}
