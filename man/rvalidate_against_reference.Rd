% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robust_implementations.R
\name{rvalidate_against_reference}
\alias{rvalidate_against_reference}
\title{Validate diagnostics against reference implementations}
\usage{
rvalidate_against_reference(test_name, model, data, tolerance = 1e-06)
}
\arguments{
\item{test_name}{Character scalar naming the diagnostic: "breusch_pagan",
"studentized_bp", or "white".}

\item{model}{A fitted \link[stats:lm]{stats::lm} object representing the mean specification
to be diagnosed.}

\item{data}{A \link[base:data.frame]{base::data.frame} (or object coercible to one) containing the
variables referenced by \code{model}. It must include the observations used to
fit \code{model} and will be checked for missing values.}

\item{tolerance}{Acceptable absolute difference between statistics and
p-values when comparing to the reference implementation.}
}
\value{
A list describing the comparison, including a status field with
values \code{"ok"}, \code{"mismatch"}, or \code{"skipped"} when the reference package
is unavailable.
}
\description{
Compares the package's test results with established implementations
from other packages (currently \code{lmtest} and \code{car}). Differences exceeding
the supplied tolerance are flagged.
}
