% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modern_diagnostics.R
\name{performQuantileRegressionTest}
\alias{performQuantileRegressionTest}
\title{Quantile regression heteroscedasticity test}
\usage{
performQuantileRegressionTest(
  model,
  data,
  taus = c(0.25, 0.75),
  se_type = c("nid", "rank")
)
}
\arguments{
\item{model}{A fitted \link[stats:lm]{stats::lm} object describing the mean structure whose
residual variance is to be assessed.}

\item{data}{A \link[base:data.frame]{base::data.frame} (or compatible object) containing the
variables referenced in \code{model}. The data must include all observations used
to fit \code{model} and should not contain unresolved missing values.}

\item{taus}{Numeric vector of quantiles to compare. The first and last values
are used to form the Wald statistic and default to 0.25 and 0.75.}

\item{se_type}{Standard error estimator passed to
\code{quantreg::summary.rq()}. Defaults to the "nid" approximation.}
}
\value{
An \link[stats:htest]{htest} object summarising the Wald test comparing
quantile-specific slopes.
}
\description{
Detects heteroscedasticity by comparing slope coefficients from two quantile
regressions. Under homoskedasticity the conditional slope should be stable
across quantiles; differences indicate variance that varies with regressors.
}
\examples{
if (requireNamespace("quantreg", quietly = TRUE)) {
  data(mtcars)
  model <- lm(mpg ~ wt + hp, data = mtcars)
  performQuantileRegressionTest(model, mtcars)
}

}
\references{
Koenker, R., & Bassett, G. (1978). Regression quantiles. \emph{Econometrica,
46}(1), 33–50.

Hao, L., & Naiman, D. Q. (2007). \emph{Quantile Regression}. SAGE.
}
