% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_methods.R
\name{rbootstrap_test_statistic}
\alias{rbootstrap_test_statistic}
\title{Bootstrap a test statistic}
\usage{
rbootstrap_test_statistic(
  test_function,
  model,
  data,
  B = 1000,
  parallel = FALSE,
  ci_level = 0.95,
  resample = c("pairs"),
  n_cores = NULL,
  progress = interactive(),
  ...
)
}
\arguments{
\item{test_function}{A function that accepts \verb{(model, data, ...)} and
returns an object with a numeric \code{statistic} element (typically an
\link[stats:htest]{htest} result).}

\item{model}{A fitted model of class \code{lm} or \code{glm}.}

\item{data}{Data frame used when fitting \code{model}.}

\item{B}{Integer, number of bootstrap replications. Defaults to 1000.}

\item{parallel}{Logical, compute replicates in parallel when possible?}

\item{ci_level}{Confidence level for the percentile interval.}

\item{resample}{Bootstrap strategy. Currently only "pairs" sampling is
supported where rows of \code{data} are sampled with replacement.}

\item{n_cores}{Optional integer specifying the number of worker processes to
use when \code{parallel = TRUE}. Defaults to \code{parallel::detectCores() - 1}.}

\item{progress}{Logical flag controlling whether a textual progress bar is
displayed during bootstrap replication.}

\item{...}{Additional arguments forwarded to \code{test_function}.}
}
\value{
A list with components:
\describe{
\item{\code{original}}{Result returned by \code{test_function} on the
original data.}
\item{\code{original_statistic}}{Numeric value of the test statistic from
the original sample.}
\item{\code{replicates}}{Numeric vector of bootstrap statistics.}
\item{\code{ci}}{Percentile confidence interval for the statistic.}
\item{\code{p_value}}{Empirical bootstrap p-value.}
\item{\code{B}}{Requested number of replications.}
\item{\code{effective_samples}}{Number of non-missing bootstrap
replications.}
\item{\code{call}}{Matched call for reproducibility.}
}
}
\description{
Provides a consistent wrapper around bootstrap resampling for test
statistics produced by heteroscedasticity diagnostics. The routine
refits the supplied model on bootstrap resamples of the working data
and evaluates \code{test_function} on each replicate. Percentile confidence
intervals and an empirical p-value are returned alongside the
bootstrap distribution.
}
\examples{
data(mtcars)
model <- lm(mpg ~ wt + cyl, data = mtcars)
if (interactive()) {
  set.seed(123)
  boot <- rbootstrap_test_statistic(performWhiteTest, model, mtcars, B = 100)
  boot$ci
}
}
