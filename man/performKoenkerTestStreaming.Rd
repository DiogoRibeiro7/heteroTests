% Generated by manual edit; keep in sync with R/performance.R
\name{performKoenkerTestStreaming}
\alias{performKoenkerTestStreaming}
\title{Streaming Koenker studentized Breusch\–Pagan test}
\usage{
performKoenkerTestStreaming(model, data, chunk_size = 10000, progress = interactive())
}
\arguments{
\item{model}{A fitted \link[stats:lm]{stats::lm} object supplying residuals and fitted values for
the diagnostic.}

\item{data}{A \link[base:data.frame]{base::data.frame} containing the variables referenced in
\code{model}. The data must align with the observations used to fit the model.}

\item{chunk_size}{Positive integer giving the number of observations processed per
streaming chunk.}

\item{progress}{Logical flag indicating whether a textual progress bar should be shown
while processing the chunks. Defaults to \code{interactive()}.}
}
\value{
A \link[stats:htest]{htest} object equivalent to \code{\link{performKoenkerTest}} with metadata
recording the streaming configuration.
}
\description{
Evaluates Koenker's studentized variant of the Breusch\–Pagan test (the
\eqn{n R^2} statistic from regressing the squared residuals on the regressors)
using streamed cross-products so that large datasets can be processed without
allocating the full auxiliary regression matrix.
}
\details{
Chunks of the data contribute to the cross-product matrices for the regression
of squared residuals on the model regressors. Aggregating these matrices yields
the exact Koenker statistic while bounding memory usage. Sparse cross-products
from \pkg{Matrix} are used automatically when available for large chunks.
}
\examples{
data(mtcars)
mod <- lm(mpg ~ wt + qsec, data = mtcars)
performKoenkerTestStreaming(mod, mtcars, chunk_size = 16, progress = FALSE)
}
\seealso{
\code{\link{performKoenkerTest}} for the standard implementation.
}
