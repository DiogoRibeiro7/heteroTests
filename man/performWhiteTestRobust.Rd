% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robust_implementations.R
\name{performWhiteTestRobust}
\alias{performWhiteTestRobust}
\title{Robust White test with bootstrap and effect sizes}
\usage{
performWhiteTestRobust(
  model,
  data,
  method = c("standard", "reduced"),
  bootstrap = FALSE,
  B = 1000,
  ci_level = 0.95,
  parallel = FALSE
)
}
\arguments{
\item{model}{A fitted \link[stats:lm]{stats::lm} object representing the mean specification
to be diagnosed.}

\item{data}{A \link[base:data.frame]{base::data.frame} (or object coercible to one) containing the
variables referenced by \code{model}. It must include the observations used to
fit \code{model} and will be checked for missing values.}

\item{method}{Character string selecting the auxiliary specification.
"standard" retains squares and cross-products, while "reduced"
excludes cross-products for high-dimensional designs.}

\item{bootstrap}{Logical, compute bootstrap diagnostics for the
statistic and p-value?}

\item{B}{Number of bootstrap replications when \code{bootstrap = TRUE}.}

\item{ci_level}{Confidence level for reported intervals.}

\item{parallel}{Logical, allow parallel bootstrap evaluation when the
\code{parallel} package is available.}
}
\value{
An object of class \code{htest} augmented with a
\code{robust_details} list containing bootstrap, effect size, and power
information.
}
\description{
Extends \code{\link[=performWhiteTest]{performWhiteTest()}} with optional bootstrap resampling, confidence
intervals, effect size reporting, and power analysis.
}
\details{
Provides enriched inference around the White test by combining bootstrap
resampling (Efron & Tibshirani, 1993) with asymptotic approximations. The
\code{robust_details} element summarises interval estimates, effect sizes, and power
calculations to aid decision-making.
}
\examples{
data(mtcars)
mod <- lm(mpg ~ wt + qsec, data = mtcars)
performWhiteTestRobust(mod, mtcars, bootstrap = TRUE, B = 200)

}
\references{
White, H. (1980). A heteroskedasticity-consistent covariance matrix estimator
and a direct test for heteroscedasticity. \emph{Econometrica, 48}(4), 817–838.

Efron, B., & Tibshirani, R. J. (1993). \emph{An Introduction to the Bootstrap}.
Chapman & Hall.
}
\seealso{
\code{\link[=performWhiteTest]{performWhiteTest()}} for the base statistic and
\code{\link[=performWhiteTestBootstrap]{performWhiteTestBootstrap()}} for a lighter-weight resampling option.
}
