This package was checked on Ubuntu, macOS and Windows via GitHub Actions.
R CMD check --as-cran returned no ERRORs, WARNINGs or NOTEs.
The package passes unit tests and the pkgdown site builds cleanly.
