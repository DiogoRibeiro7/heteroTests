# Check memory usage and warn for large datasets

Check memory usage and warn for large datasets

## Usage

``` r
check_memory_usage(data, threshold_mb = 100)
```

## Arguments

- data:

  The dataset to check

- threshold_mb:

  Memory threshold in MB (default: 100MB)
